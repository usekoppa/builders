import { Interactions } from "../api";
import { Commands } from "../api/commands";
import { Command, Subcommand, SubcommandGroup } from "../application_commands";

export class InteractionRequest {
  constructor(raw: Interactions.Incoming.Interaction) {
    
  }
}
